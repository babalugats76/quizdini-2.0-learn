import React from 'react';
import logo from '../../../src/spinner-logo.svg';

const Spinner = () => {
  return <img className="logo-spinner" src={logo} alt="Quizdini Logo" />;
};

export default Spinner;
